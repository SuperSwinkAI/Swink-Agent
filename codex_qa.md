| Priority | Type | Finding | Evidence | Why it matters | Simplification / replacement | GH Issue |
| --- | --- | --- | --- | --- | --- | --- |
| P1 | correctness | `PreDispatchPolicy` promises loop context it does not actually receive. | `src/policy.rs:62`; `src/loop_/tool_dispatch.rs:155`; `src/loop_/tool_dispatch.rs:156`; `src/loop_/tool_dispatch.rs:157`; `src/loop_/tool_dispatch.rs:159` | Policies can silently make the wrong decision because tool dispatch fabricates `turn_index`, usage, cost, message count, and overflow state with placeholder values. That is a correctness trap, not just an awkward API. | Either thread the real loop snapshot into tool dispatch or replace the shared `PolicyContext` here with a smaller `ToolDispatchContext` that only exposes data the runtime actually maintains. Prefer the smaller context if full loop state is not needed. | [#166](https://github.com/SuperSwinkAI/Swink-Agent/issues/166) |
| P1 | correctness | Gemma 4 delimiter parsing slices UTF-8 strings by byte offset in two duplicated helpers. | `local-llm/src/stream.rs:238`; `local-llm/src/stream.rs:241`; `local-llm/src/stream.rs:438`; `local-llm/src/stream.rs:441`; `local-llm/src/stream.rs:120`; `local-llm/src/stream.rs:320` | A non-ASCII chunk near a split `<|channel>thought` or `<|tool_call>` delimiter can panic the local streaming path. Because the same helper is duplicated for thought and tool-call parsing, the bug surface and maintenance cost are doubled. | Keep the custom Gemma 4 parser, but replace both copies with one shared UTF-8-safe delimiter scanner that works on byte slices or `char_indices()`, then add regression tests for Unicode content across chunk boundaries. | [#171](https://github.com/SuperSwinkAI/Swink-Agent/issues/171) |
| P2 | maintainability | Session persistence is still split across overlapping backends and contracts. | `memory/src/store_async.rs:32`; `memory/src/store_async.rs:114`; `memory/src/store_async.rs:155`; `memory/src/jsonl.rs:1`; `memory/src/jsonl.rs:348`; `memory/src/jsonl.rs:481`; `memory/src/sqlite.rs:57`; `memory/src/sqlite.rs:246`; `tui/src/app/lifecycle.rs:12`; `tui/src/app/lifecycle.rs:26` | One responsibility now spans a sync trait, an async mirror trait, a spawn-blocking adapter that ignores part of its signature, a corruption-prone JSONL protocol, and a separate SQLite backend. The overlap increases maintenance cost and keeps surprising behavior at the API boundary. | Collapse onto one canonical persistence shape. Smallest credible path: make SQLite the default durable backend, demote JSONL to migration/import-export or legacy support, remove the mirrored async trait in favor of one async-first contract, and keep encoding logic centralized instead of backend-specific. | [#165](https://github.com/SuperSwinkAI/Swink-Agent/issues/165) |
| P2 | dependency / packaging | The Bedrock adapter carries security-critical handwritten SigV4 signing inside an already oversized provider module. | `adapters/src/bedrock.rs:322`; `adapters/src/bedrock.rs:343`; `adapters/src/bedrock.rs:351`; `adapters/src/bedrock.rs:358`; `adapters/src/bedrock.rs:895`; `adapters/src/bedrock.rs:916`; `adapters/src/bedrock.rs:925`; `adapters/src/bedrock.rs:949` | Custom request-signing code is expensive to maintain and easy to get subtly wrong. Keeping canonical header construction, key derivation, and authorization assembly inline with request shaping and event parsing makes the Bedrock adapter harder to evolve safely. | Replace only the signing responsibility with the AWS-maintained `aws-sigv4` crate and isolate it in a focused helper/module. Keep the existing Bedrock request mapping and event-stream parser where custom logic is still justified. | [#172](https://github.com/SuperSwinkAI/Swink-Agent/issues/172) |

| Order | Refactor | Estimated effort | Regression risk | GH Issue link |
| --- | --- | --- | --- | --- |
| 1 | Make pre-dispatch policy context truthful before adding more policies or plugin hooks. | Medium | Medium | [#166](https://github.com/SuperSwinkAI/Swink-Agent/issues/166) |
| 2 | Fix the Gemma 4 UTF-8 panic path while the parser surface is still relatively localized. | Medium | Medium | [#171](https://github.com/SuperSwinkAI/Swink-Agent/issues/171) |
| 3 | Replace bespoke Bedrock signing with `aws-sigv4` before more adapter-specific auth logic accumulates around it. | Medium | Medium | [#172](https://github.com/SuperSwinkAI/Swink-Agent/issues/172) |
| 4 | Collapse memory persistence onto one canonical backend/contract after the smaller correctness and packaging fixes land. | Large | High | [#165](https://github.com/SuperSwinkAI/Swink-Agent/issues/165) |
